# Rendered to Caddyfile by scripts/install-stacks.sh (do not edit on the server).
# Site blocks are generated from stack folders — see scripts/generate-caddyfile.sh.
# Variables come from setup.env — see setup.env.example.

{
    email ${ACME_EMAIL}
}
