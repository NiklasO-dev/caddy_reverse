# Default subdomain would be "uptime-kuma"; keep the short hostname.
SUBDOMAIN=kuma
PORT=3001
UPSTREAM=uptime-kuma
BASIC_AUTH=true
COMMENT="Uptime Kuma - Caddy basic auth plus Kuma login after the gate"
